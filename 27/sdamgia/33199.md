#### Решение #1 (Python)
##### Файл A
```python
f = open('C:/Users/mirea/Desktop/27-A.txt', 'r')

# общее количество троек в наборе
n = int(f.readline())

ans = [0, 0, 0]

for x in range(n):
    a = [int(i) for i in f.readline().split()]

    # берём из списка максимальное число
    a.sort()
    m = a.pop()

    ans[0] += a[0]
    ans[1] += a[1]

    ans[2] += m

# если первая группа нечётная, а вторая - чётная, то просто меняем местами
    if ans[0] % 2 != 0 and ans[1] % 2 == 0:
        ans[0], ans[1] = ans[1], ans[0]

# проверка каждой кучи на удолитворимость условию задачи(должно быть True True)
# и вывод ответа
print( ans[0] % 2 == 0, ans[1] % 2 > 0 )
print( ans[2] )
```
**Ответ:** 541.
##### Файл B
```python
f = open('C:/Users/mirea/Desktop/27-B.txt', 'r')

# общее количество троек в наборе
n = int(f.readline())

ans = [0, 0, 0]
save = 9999999999999999999999999

for x in range(n):
    a = [int(i) for i in f.readline().split()]

    # берём из списка максимальное число
    a.sort()
    m = a.pop()

    ans[0] += a[0]
    ans[1] += a[1]

    ans[2] += m

    delta = []

    for i in range(2):
        candidate = m - a[i]

        if candidate % 2 > 0:
            delta.append(candidate)
    
    if len(delta) > 0:
        save = min(save, min(delta))

# если первая группа нечётная, а вторая - чётная, то просто меняем местами
    if ans[0] % 2 != 0 and ans[1] % 2 == 0:
        ans[0], ans[1] = ans[1], ans[0]

# проверка каждой кучи на удолитворимость условию задачи(должно быть True True)
# и вывод ответа
print( ans[0] % 2 == 0, ans[1] % 2 > 0 )

print( ans[2] - save )
```
**Ответ:** 300229428.
